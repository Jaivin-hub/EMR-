import React from 'react'

function Input({ onChange, value, style }) {
    return (
        <div>
            {/* <StyledInput onChange={(e) => { onChange(e) }} /> */}
        </div>
    )
}

export default Input